name: Apple macOS
on: { push: { branches: [main, lab] }, pull_request: {}, workflow_dispatch: {} }

permissions:
  contents: read

concurrency:
  group: ci-${{ github.repository }}-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  test:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v6
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "26.5"
      - name: Check Swift Version
        run: swift --version
      - run: xcodebuild -resolvePackageDependencies -scheme "Euryale"
      - run: xcodebuild test -scheme "Euryale" -destination "platform=macOS"
